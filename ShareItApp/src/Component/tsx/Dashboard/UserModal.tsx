import { FC } from "react";
import { User } from "../../../Entities/Users";

interface Props {
    currentUser: User,
    handhandleClickOpen() : void,
    handleClickOpen() : void,
    onChange (e: React.ChangeEvent<HTMLInputElement>): void
}

export const UserModal: FC = () => {

    return (
        <div>cos</div>
    )
}